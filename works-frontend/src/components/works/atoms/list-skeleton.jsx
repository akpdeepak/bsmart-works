// Convenience re-export — the canonical ListSkeleton lives alongside its Skeleton atom.
// Import from here so consumers don't need to know the internal atom file layout:
//   import { ListSkeleton } from '@/components/works/atoms/list-skeleton';
export { ListSkeleton } from './skeleton';

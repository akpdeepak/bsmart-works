<?xml version="1.0" encoding="UTF-8"?>
<!--
  bSmart Works — structured logging configuration (CLAUDE.md §14).
  - local / test : human-readable console output (default Spring pattern)
  - staging / prod : JSON per line — ready for log aggregation (Loki, CloudWatch, etc.)
  Switch by setting SPRING_PROFILES_ACTIVE=staging or production.
-->
<configuration>

  <!-- ── Human-readable console (local + test) ─────────────────────────── -->
  <springProfile name="default,local,test">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) - %msg%n</pattern>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
    <!-- Quieten noisy Spring internals locally -->
    <logger name="org.springframework.web" level="WARN"/>
    <logger name="org.hibernate.SQL"       level="DEBUG"/>
    <logger name="com.bcits.works"        level="DEBUG"/>
  </springProfile>

  <!-- ── JSON console (staging + production) ───────────────────────────── -->
  <springProfile name="staging,production">
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
        <!--
          Simple JSON-ish line. Replace with logstash-logback-encoder for full ECS/GELF output
          once a log aggregator is wired up (CLAUDE.md §14.3).
        -->
        <pattern>{"ts":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}","level":"%-5level","thread":"%thread","logger":"%logger{36}","msg":"%msg","exception":"%ex{short}"}%n</pattern>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="JSON_CONSOLE"/>
    </root>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate"       level="WARN"/>
    <logger name="com.bcits.works"    level="INFO"/>
  </springProfile>

</configuration>

# ESLint 10 is ahead of some plugins' declared peer ranges (e.g. eslint-plugin-jsx-a11y
# still caps at eslint ^9 though it works fine on 10). Relax peer resolution so install
# and CI `npm ci` agree. Revisit when the plugins bump their peer ranges.
legacy-peer-deps=true
